<Page
    x:Class="Lab5.Donut"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab5"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="Red">
        <Grid.RowDefinitions>
            <RowDefinition Height="175"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image Source="Assets/DonutLogo1.png"/>
        <TextBlock Text="Original Glazed Count:" Style="{StaticResource TextStyle}" Grid.Row="1"/>
        <Slider Maximum="24" Margin="0,0,20,0" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1"/>
        <TextBlock Text="Speedway Special Count:" Style="{StaticResource TextStyle}" Grid.Row="2"/>
        <Slider Maximum="24" Margin="0,0,20,0" VerticalAlignment="Center" Grid.Row="2" Grid.Column="1"/>
    </Grid>
</Page>
