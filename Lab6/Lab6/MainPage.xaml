<Page
    x:Class="Lab6.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab6"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Lab6.ViewModels"
    mc:Ignorable="d">

    <Grid Background="LightBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center">
            <AutoSuggestBox Name="LocationAutoSuggestBox" ItemsSource="{x:Bind ViewModel.AutoCompleteNames}" HorizontalAlignment="Center" QueryIcon="Find" PlaceholderText="Enter Location..." Width="300" TextChanged="LocationAutoSuggestBox_TextChanged" QuerySubmitted="LocationAutoSuggestBox_QuerySubmitted"/>
            <Image Name="ResultImage" Width="200" Height="200" Style="{StaticResource ImageStyle}">
                <Image.Source>
                    <BitmapImage UriSource="{x:Bind ViewModel.ImageUrl, Mode=OneWay}"/>
                </Image.Source>
            </Image>
            <TextBlock Name="TemperatureTextBlock" Text="{x:Bind ViewModel.Temperature, Mode=OneWay}" FontSize="52" Style="{StaticResource MainTextStyle}"/>
            <TextBlock Name="DescriptionTextBlock" Text="{x:Bind ViewModel.Description, Mode=OneWay}" FontSize="36" Style="{StaticResource MainTextStyle}"/>
            <TextBlock Name="LocationTextBlock" Text="{x:Bind ViewModel.LocationName, Mode=OneWay}" FontSize="24" Style="{StaticResource MainTextStyle}" TextWrapping="Wrap"/>
        </StackPanel>

        <GridView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Forecast}" HorizontalAlignment="Center">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="data:ForecastDayViewModel">
                    <StackPanel Margin="20,0,20,0">
                        <TextBlock Text="{x:Bind Date}" Style="{StaticResource ForecastTextStyle}"/>
                        <TextBlock Text="{x:Bind TemperatureRange}" Style="{StaticResource ForecastTextStyle}"/>
                        <Image Source="{x:Bind IconUrl}" Width="150"/>
                        <TextBlock Text="{x:Bind ForecastDescription}" Style="{StaticResource ForecastTextStyle}" MinHeight="50" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

    </Grid>
</Page>
