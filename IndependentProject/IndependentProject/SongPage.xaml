<Page
    x:Class="IndependentProject.SongPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IndependentProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="Title" Text="{x:Bind infoViewModel.Title}" Style="{StaticResource PageTitleStyle}"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="SearchPageButton" Content="Search" Style="{StaticResource BackButtonStyle}" Click="SearchPageButton_Click"/>
            <Button Name="BackButton" Style="{StaticResource BackButtonStyle}" Click="BackButton_Click"/>
        </StackPanel>
        <TextBlock Name="AlbumName" Grid.Row="1" Text="{x:Bind infoViewModel.AlbumName, Mode=OneWay}" Margin="20,0,0,0"/>
        <TextBlock Name="Lyrics" Grid.Row="2" Margin="20,0,0,0"/>
    </Grid>
</Page>
